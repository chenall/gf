name: 合并更新到主分支

on:
  push:
    branches: [ "upstream" ]
   
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: tgymnich/fork-sync@v1.3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          base: master
          head: upstream